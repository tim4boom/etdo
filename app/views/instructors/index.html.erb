<div class="general-banner" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.2)), url(<%= asset_path 'divers.jpg'%>)"></div>
<div class="trapezoid">
  <div class="banner-container">
    <h2 class="general-banner-header">STANDORTE DER INSTRUKTOREN</h2>
    <p class="general-banner-text">Karte mit Standorten</p>
  </div>
</div>
<div class="general-container">
  <div class="header">
    <h1>Unsere Instruktoren</h1>
  </div>

  <div class="avatars">
    <% @instructors.each do |instructor| %>
      <% if instructor.avatar.attached? %>
        <div class="avatar-container">
          <div class="instructor-avatar" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= rails_blob_url(instructor.avatar) %>')">
            <%= render 'shared/instructor-edit', instructor: instructor %>
          </div>
          <%= render 'shared/instructor-avatar', instructor: instructor %>
        </div>
      <% else %>
        <div class="avatar-container">
          <div class="instructor-avatar" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path 'default-avatar.jpeg'%>)">
            <%= render 'shared/instructor-edit', instructor: instructor %>
          </div>
          <%= render 'shared/instructor-avatar', instructor: instructor %>
        </div>
      <% end %>
    <% end %>
  </div>

  <%= render "shared/arrow-up" %>
</div>
